# Bot identity (customize these for your instance)
BOT_NAME=MyBot             # Display name (e.g., "RedBot", "Jarvis")
BOT_USER=mybot             # Linux username the bot runs as
BOT_EMOJI=🤖               # Bot's emoji identity

# User info
USER_NAME=YourName
USER_TIMEZONE=UTC           # e.g., MST, EST, PST, UTC
USER_LOCATION=City, State   # e.g., Denver, CO
USER_EMAIL=you@example.com

# API Keys (all free tier)
NVIDIA_API_KEY=           # From build.nvidia.com
MEM0_API_KEY=             # From mem0.ai
BRAVE_SEARCH_KEY=         # From brave.com/search/api
VERCEL_AI_KEY=            # From sdk.vercel.ai/gateway

# Telegram (optional -- leave blank to skip Telegram setup)
TELEGRAM_BOT_TOKEN=       # From @BotFather
TELEGRAM_USER_ID=         # Your numeric Telegram user ID

# Gateway (auto-generated if blank)
GATEWAY_PORT=18789
GATEWAY_TOKEN=            # Auto-generated during setup if blank

# gog (Google Workspace CLI)
GOG_KEYRING_PASSWORD=     # Password for gog file keyring (set any value)

# SFTP Backup (leave blank to disable offsite backup)
SFTP_HOST=                # SFTP server hostname or IP
SFTP_PORT=22              # SFTP port (default 22)
SFTP_USER=                # SFTP username
SFTP_PASS=                # SFTP password (leave blank for key-based auth)
SFTP_KEY=                 # Path to SSH private key (leave blank for password auth)
SFTP_REMOTE_PATH=/backups # Remote directory for backups
SFTP_RETENTION_DAYS=30    # Days to keep backups on SFTP server
